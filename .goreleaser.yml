version: 2

project_name: purtypics

before:
  hooks:
    - go mod tidy

builds:
  - id: purtypics
    main: ./main.go
    binary: purtypics
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
      - arm
      - 386
    goarm:
      - 6
      - 7
    ignore:
      - goos: windows
        goarch: arm
      - goos: windows
        goarch: arm64
      - goos: darwin
        goarch: 386
      - goos: darwin
        goarch: arm

archives:
  - id: purtypics
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- if eq .Os "darwin" }}macOS{{ else }}{{ .Os }}{{ end }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - LICENSE*
      - README*
      - CHANGELOG*

dockers:
  - image_templates:
      - "ghcr.io/chrissnell/purtypics:{{ .Tag }}-amd64"
    dockerfile: Dockerfile
    use: docker
    goos: linux
    goarch: amd64
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source={{.GitURL}}"
  - image_templates:
      - "ghcr.io/chrissnell/purtypics:{{ .Tag }}-arm64"
    dockerfile: Dockerfile
    use: docker
    goos: linux
    goarch: arm64
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source={{.GitURL}}"

docker_manifests:
  - name_template: "ghcr.io/chrissnell/purtypics:{{ .Tag }}"
    image_templates:
      - "ghcr.io/chrissnell/purtypics:{{ .Tag }}-amd64"
      - "ghcr.io/chrissnell/purtypics:{{ .Tag }}-arm64"
  - name_template: "ghcr.io/chrissnell/purtypics:latest"
    image_templates:
      - "ghcr.io/chrissnell/purtypics:{{ .Tag }}-amd64"
      - "ghcr.io/chrissnell/purtypics:{{ .Tag }}-arm64"

checksum:
  name_template: 'checksums.txt'

release:
  # Allows overwriting existing releases
  draft: false
  replace_existing_draft: true
  replace_existing_artifacts: true
  target_commitish: '{{ .Commit }}'
  prerelease: auto
  mode: replace

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'